# Prompt 9 verification

SAP import implements the versioned contract, hostile parser, rejected-file
queue, site-grouped preview, select-all/individual selection, exact staged commit,
completion results and F11 reconciliation. Evidence: `tests/test_sapimport.py`.
